# 複製為 .env 後填入實際金鑰:cp .env.example .env
# .env 已列入 .gitignore,不會進版控;已存在的環境變數(export)優先於 .env。
# config 中以 ${ENV_VAR} 引用(見 configs/default.yaml 的公司整合區塊)。

# 公司環境(embedding / LLM 閘道 / rerank API,依實際啟用的方法填)
# COMPANY_EMBEDDING_API_KEY=請填入公司 embedding API 金鑰
# COMPANY_LLM_API_KEY=請填入公司 LLM 閘道金鑰
# COMPANY_RERANK_API_KEY=請填入公司 rerank API 金鑰

# Elasticsearch(indexing: elasticsearch 或跑 -m es 整合測試時需要)
# ES_URL=http://localhost:9200
# 叢集開了 security(公司 / 雲端叢集預設如此)時,認證二選一;
# 對應的 config 欄位在 indexing.params(見 configs/default.yaml)。
# ES_USERNAME=elastic
# ES_PASSWORD=請填入密碼
# ES_API_KEY=請填入 base64 的 id:api_key
# ES_CA_CERTS=/path/to/ca.crt   # https 且憑證由私有 CA 簽發時

# 個人環境(選配)
# OPENAI_API_KEY=sk-...

# Haystack 匿名遙測預設已由 rag/__init__.py 關閉(封閉網路下連 eu.i.posthog.com
# 會拋 WinError 10061 之類的連線錯誤)。要開啟請設真環境變數,寫在這裡無效:
# 這個變數在 import haystack 當下就被讀取,而 .env 要到 load_config() 才載入。
#   PowerShell:  $env:HAYSTACK_TELEMETRY_ENABLED = "True"
#   bash:        export HAYSTACK_TELEMETRY_ENABLED=True
