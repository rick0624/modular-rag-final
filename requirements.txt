# 基本依賴:離線可跑(mock embedding + in_memory 索引 + mock LLM),
# 不需網路、金鑰或模型下載。與 pyproject.toml 的 dependencies + [dev] 同步。
#
#   pip install -r requirements.txt      # 請在 repo 根目錄執行(下方 -e . 才裝得到)

# 本專案自己(editable 安裝):裝了之後,自己的腳本 / notebook 才能
# import rag(否則會遇到 No module named 'rag');改 rag/ 程式碼立即生效。
-e .

haystack-ai>=2.31,<3.0
pydantic>=2.5
PyYAML>=6.0
httpx>=0.27          # api_embedding / api_rerank / gateway generator 的 HTTP client
pypdf[crypto]>=4.0   # PDF 文字層抽取;[crypto] = AES 加密 PDF(常見於公文)需要
networkx>=3.0        # rag/trace.py 的步驟排序(haystack 亦相依,此處顯式宣告)

# 測試
pytest>=7.4
